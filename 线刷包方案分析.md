# 晶晨线刷包（Amlogic USB Burning Tool）可行性分析

日期：2026-09-19
对象：SKYWORTH E900V22C（S905L3AB，4G 运存）

---

## 一、结论先说

**能做，Amlogic 烧录包格式我已经完全破解**，自研的 Python 工具实测解包成功、CRC 校验通过。

**但真正的障碍不是打包，是 bootloader。** 这决定了成品是"大概率能用"还是"大概率变砖"。

---

## 二、格式已完全掌握（实测证据）

参考 `superna9999/pyamlboot` 的 `AML-IMAGE-FORMAT.md`，用它自研了 `work/amlpkg.py`。

对 `22C第三版 修复版.img` 实测解析结果：

```
crc = 0x5BE0F09D      version = 2      magic = 0x27B51956  OK
size = 1,760,074,508  align = 4        items = 24
CRC 校验 = OK
```

24 个分区全部读出：

| # | 类型 | main | sub | offset | size |
|---|---|---|---|---|---|
| 0 | normal | USB | DDR | 13,888 | 1,387,376 |
| 1 | normal | USB | UBOOT | 13,888 | 1,387,376 |
| 2 | normal | PARTITION | _aml_dtb | 1,401,264 | 64,266 |
| 4 | normal | PARTITION | **boot** | 1,465,580 | 11,589,632 |
| 6 | normal | PARTITION | **bootloader** | 13,888 | 1,387,376 |
| 10 | normal | PARTITION | logo | 13,055,796 | 1,040,512 |
| 13 | sparse | PARTITION | odm | 14,096,356 | 2,597,060 |
| 15 | normal | conf | platform | 16,693,464 | 155 |
| 16 | normal | PARTITION | product | 16,693,620 | 134,217,728 |
| 18 | normal | PARTITION | recovery | 150,911,396 | 24,915,240 |
| 20 | normal | PARTITION | **system** | 175,826,684 | 1,342,177,280 |
| 22 | sparse | PARTITION | vendor | 1,518,004,012 | 242,070,448 |

格式要点（v2）：
- 头 64 字节：`crc`(4, =`crc32(data[4:]) ^ 0xffffffff`) `version`(4) `magic`(4, `0x27B51956`) `size`(8) `align`(4) `count`(4) `reserved`(36)
- 描述符 576 字节 × N：`id`(4) `type`(4) `unk`(8) `offset`(8) `size`(8) `main[256]` `sub[256]` `verify`(4) `is_backup`(2) `backup_id`(2) `reserved`(24)
- 数据区从第一个描述符的 `offset` 开始

**也就是说：解包、改分区、重打包这条链路是通的，没有技术障碍。**

---

## 三、真正的障碍：bootloader

对比两套 u-boot（`strings` 实测）：

| | 安卓包 bootloader (item 6) | FnNAS `u-boot-e900v22c.bin` |
|---|---|---|
| 版本 | **U-Boot 2015.01** (Dec 30 2021) | **U-Boot 2022.04** (Apr 22 2022) |
| 标识 | Amlogic-secure-boot-module-v0.4 | 明写 `e900v22c` |
| `booti` | 1 | 1 |
| `bootm` | — | 1 |
| `fatload` | — | 1 |
| `ext4load` | — | 1 |
| `aml_autoscript` | **0** | — |
| `u-boot.ext` | **0** | — |
| ARB 防回滚 | BL30/BL31/BL33 ARB check | — |
| secure boot | 有（internal RSA key） | — |

**关键**：FnNAS 那个 `u-boot-e900v22c.bin` 是**二级引导**（bootloader 里的 `u-boot.ext`），
它必须先由**原厂 u-boot** 加载起来、交棒给它。
而原厂这套 2015.01 是裁剪过的安卓专用版本，命令集里没有 `aml_autoscript` / `u-boot.ext` 的痕迹。

**这基本解释了你 U 盘启动失败的原因** —— 不是 dtb 问题，是这套 bootloader 不认 U 盘的启动脚本。

`_aml_dtb`（gzip 压缩的 `AML_` 多 dtb 容器，解压 258048 字节，含 3 个 FDT）里保存着分区表，
改分区布局必须动它。

---

## 四、三个方案

### 方案 A：线刷安卓底包 → U 盘启动一次 → 写 eMMC　【推荐】

```
线刷 4G 安卓包（你已有，bootloader 由它保证）→ 进安卓开 ADB
→ adb shell reboot update（或复位键）触发 U 盘启动
→ FnNAS 起来后：fnnas-install -m yes -a no
→ 拔 U 盘，系统从 eMMC 启动
```

- **成功率最高**，这是 ophub 官方路径
- U 盘只需要用**一次**，之后完全脱离
- 不碰 bootloader，没有变砖风险
- 前提：底包的 bootloader 要支持 U 盘启动。你手上那两个包里，"22C第三版 修复版"这类第三方包通常已经换了支持 U 盘启动的 bootloader

### 方案 B：混合线刷包（安卓底 + FnNAS 系统）

保留安卓包的 `USB/DDR/UBOOT/bootloader/_aml_dtb/platform.conf`，替换：
- `boot` 分区 → 把 FnNAS 的 `Image` + `uInitrd` 打成 **Android boot.img**（u-boot 原生认 `ANDROID!`）
- `system`/`product` → FnNAS 的 rootfs（btrfs 转 raw）
- `_aml_dtb` → 换成果 4G 版 dtb 重新封装

- 格式上完全可行（工具我已就绪）
- 但依赖原厂 u-boot 能正常引导非安卓内核，**成功率不确定**
- 需要反复试错，每次刷机 + 上电
- 分区表改错有变砖风险（可短接 USB 救回）

### 方案 C：全自制线刷包（连 bootloader 一起换）

- 要把 FnNAS 的 u-boot 2022.04 塞进 FIP（BL33）
- 面对 **ARB 防回滚检查 + secure boot RSA 校验**
- **变砖概率很高**，救砖需要短接 + TTL

---

## 五、我的建议

**先走方案 A**。你 U 盘启动失败大概率是底包 bootloader 的问题，换一个支持 U 盘启动的安卓底包重刷就能过。
只要你告诉我**失败时的具体现象**（黑屏 / 卡 logo / 进 recovery / U 盘灯不闪），我能很快定位。

**如果坚持要线刷包**，我做方案 B —— 工具链和格式都已就绪，但请先确认你能接受：
1. 需要多次试错
2. 有变砖风险（虽可用 USB Burning Tool 短接救回）

---

## 六、本次产出的工具

| 文件 | 说明 |
|---|---|
| `work/amlpkg.py` | 自研 Amlogic 烧录包解析/解包/校验工具（纯 Python，已实测可用） |
| `work/amldtb.py` | `_aml_dtb` 多 dtb 容器解析 |
| `tools/aml_image_v2_packer` | 原版工具（32 位 i386，WSL 跑不了，留作参考） |
| `tools/pyamlboot/` | 格式文档 + 官方 Python 参考实现 |

解包命令：
```bash
python work/amlpkg.py info    <xxx.img>              # 查看分区表 + CRC 校验
python work/amlpkg.py extract <xxx.img> <输出目录>    # 提取所有分区
```
